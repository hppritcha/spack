packages:
  all:
    compiler: [gcc, intel, pgi, clang, xl, nag]
    providers:
      #
      #blas: [intel-mkl]
      #lapack: [intel-mkl]
      #
      #blas: [openblas]
      #lapack: [openblas]
      #
      blas: [netlib-lapack]
      lapack: [netlib-lapack]
      #
      mpi: [openmpi]
      pil: [py-pillow]
      #
      awk: [gawk]
      daal: [intel-daal]
      elf: [elfutils]
      golang: [gcc]
      ipp: [intel-ipp]
      java: [jdk]
      mkl: [intel-mkl]
      mpe: [mpe2]
      opencl: [pocl]
      openfoam: [openfoam-com, openfoam-org, foam-extend]
      pkgconfig: [pkgconf]
      scalapack: [netlib-scalapack]
      szip: [libszip, libaec]
      tbb: [intel-tbb]
      jpeg: [libjpeg-turbo, libjpeg]
  # # # # #
  automake:
    paths:
      automake: /usr
    buildable: false
  # # # # #
  autoconf:
    paths:
      autoconf: /usr
    buildable: false
  # # # # #
  bison:
    paths:
      bison: /usr
    buildable: false
  # # # # #
  boost:
    variants: [+mpi]
  # # # # #
  bzip2:
    paths:
      bzip2: /usr
    buildable: false
  # # # # #
  calico:
    variants: [+mpi]
    version: [180611]
  # # # # #
  curl:
    paths:
      curl: /usr
    buildable: false
  # # # # #
  eospac:
    version: [6.4.0beta.2]
  # # # # #
  flex:
    paths:
      flex: /usr
    buildable: false
  # # # # #
  gettext:
    paths:
      gettext: /usr
    buildable: false
  # # # # #
  git:
    paths:
      git: /usr
    buildable: false
  # # # # #
  glib:
    paths:
      glib: /usr/lib64
    buildable: false
  # # # # #
  hdf5:
    variants: [+fortran, +mpi]
    version: [1.8.17]
  # # # # #
  # help2man:
  #   paths:
  #     autoconf: /usr
  #   buildable: false
  # # # # #
  hypre:
    variants: [~internal, +mpi, -superlu]
  # # # # #
  json-c:
    paths:
      json: /usr/lib64
    buildable: False
  # # # # #
  libjpeg-turbo:
    paths:
      libjpeg: /usr/lib64
    buildable: False
  # # # # #
  libsigsegv:
    paths:
      libsigsegv: /usr/lib64
    buildable: False
  # # # # #
  libssh2:
    paths:
      libssh2: /usr/lib64
    buildable: False
  # # # # #
  libtool:
    paths:
      libtool: /usr
    buildable: False
  # # # # #
  libxml2:
    paths:
      libxml2: /usr
    buildable: False
  # # # # #
  llvm:
    variants: +link_dylib
  # # # # #
  lz4:
    paths:
      lz4: /usr/lib64
    buildable: False
  # # # # #
  m4:
    paths:
      m4: /usr
    buildable: False
  # # # # #
  mesa:
    variants: [+llvm]
  # # # # #
  metis:
    variants: [+int64, +real64]
  # # # # #
  munge:
    paths:
      munge: /usr/lib64
    buildable: False
  # # # # #
  mpc:
    paths:
      mpc: /usr/lib64
    buildable: False
  # # # # #
  mpfr:
    paths:
      mpfr: /usr/lib64
    buildable: False
  # # # # #
  ncurses:
    paths:
      ncurses: /usr
    variants: [~symlinks, ~termlib]
    buildable: False
  # # # # #
  ndi:
    version: [2.1.3beta]
  # # # # #
  numactl:
    paths:
      numactl: /usr
    buildable: false
  # # # # #
#  openmpi:
#    variants: [~memchecker, ~pmi]
#    version: [3.1.2,2.1.5]
  # # # # #
  openssl:
    paths:
      openssl: /usr
    variants: [+systemcerts]
    buildable: false
  # # # # #
  paraview:
    variants: [+mpi, +osmesa, +python]
    version: [5.4.0]
  # # # # #
  perl:
    paths:
      perl: /usr
    buildable: false
  # # # # #
  pkg-config:
    paths:
      pkg-config: /usr
    buildable: False
  # # # # #
  py-matplotlib:
    variants: [~tk]
  # # # # #
  py-pillow:
    version: [3.2.0]
  # # # # #
  rewrite:
    version: [180313]
  # # # # #
  shapo:
    variants: [+mpi, +paraview]
    version: [171108]
  # # # # #
  silo:
    variants: [+mpi]
    version: [4.10.2-bsd]
  # # # # #
  tar:
    paths:
      tar: /usr
    buildable: False
  # # # # #
  tcl:
    paths:
      tcl: /usr/lib64
    buildable: False
  # # # # #
  tk:
    paths:
      tk: /usr/lib64
    buildable: False
  # # # # #
  xz:
    paths:
      xz: /usr
    buildable: false
  # # # # #
  zip:
    buildable: False
    paths:
      zip: /usr
  # # # # #
  cmake:
    modules:
      cmake: cmake/3.13.4
    buildable: false
  # # # # #
  openmpi:
    modules:
      openmpi: openmpi/3.1.3-gcc_8.2.0
    buildable: false
  # # # # #
  zoltan:
    variants: [+parmetis]
